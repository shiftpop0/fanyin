# 8885 复用 6006 原生转译能力实施计划

计划日期：2026-08-30
计划状态：R9 已完成本地代码与迁移材料，待 Ubuntu 生产验证
运行环境：Ubuntu 服务端、Docker、单张 4090、单模型进程
本地实施阶段不启动模型、不连接或修改生产服务；真实准确率和性能由 Ubuntu 生产
环境验收确认。

## 一、背景与已确认事实

### 1.1 生产部署结构

根据根目录 `README.md`、`tailect/README.md` 和当前部署脚本，生产结构是：

```text
Windows 内网客户端
    ├─ :6006 → Ubuntu FastAPI 通用接口
    └─ :8885 → Ubuntu Nginx → :6006 同一 FastAPI 平台接口

Ubuntu 单模型进程
    ├─ Tailect_V4.1 / Qwen3-ASR
    ├─ TargetDiarization
    ├─ Qwen3-ForcedAligner
    └─ vLLM，单张 4090
```

8885 没有加载第二份模型。Nginx 使用 host network，并把允许的平台路径代理给同一个 6006 FastAPI 进程。

### 1.2 已确认的两条测试请求

6006 原生通用接口：

```http
POST http://12.33.114.138:6006/asr?diarization=true
Content-Type: multipart/form-data
file=0051409.wav; type=audio/wav
```

8885 平台接口：

```http
POST http://12.33.114.138:8885/v1/audiototext
  ?model=Tailect_V4.1
  &diarize=1
  &language=auto
  &max_chars=40
Content-Type: multipart/form-data
file=0051409.wav
```

两次请求使用同一 WAV 文件。6006 返回完整的 `speaker_segments`；8885 返回平台要求的 `code/language/data/file_name/message/uuid`。

8885 命令中上传 MIME 写成了 `video/mp4`，而文件实际是 WAV。当前后端不使用 multipart 的 MIME 字段决定解码方式，而是检查文件名和 WAV 文件头，因此它不是本次识别差异的原因。不过复测命令应改为 `type=audio/wav`，并去掉文件路径和分号之间的空格，避免未来客户端或框架版本产生歧义。

## 二、为什么同样经过 6006，处理流程仍然不同

### 2.1 Nginx 转发的是 HTTP 请求，不是“调用 6006 的默认识别逻辑”

8885 当前配置是：

```nginx
location = /v1/audiototext {
    proxy_pass http://127.0.0.1:6006;
}
```

`proxy_pass` 没有配置 URI 改写，所以请求路径会保持为：

```text
/v1/audiototext?model=...&diarize=1&...
```

FastAPI 收到请求后按“路径 + 方法”选择 Python 路由：

```text
POST /asr                  → api_server.asr_api()
POST /v1/audiototext       → v1_router.audio_to_text()
```

因此，“都到达 6006”只表示进入同一个进程、共用同一份模型，并不表示执行同一个业务函数。

### 2.2 当前两个路由分别调用了不同的服务方法

6006 `/asr?diarization=true`：

```text
api_server.asr_api()
  → UnifiedService.diarization_then_asr()
  → diarization_only()
  → 裁剪说话人片段
  → ASRWrapper.transcribe_batch()
  → 返回 speaker_segments + overall_text
```

8885 `/v1/audiototext?diarize=1`：

```text
v1_router.audio_to_text()
  → transcribe_platform_audio()
  → UnifiedService.asr_raw()              # 先整段 ASR
  → UnifiedService.forced_align()          # 全文对齐
  → UnifiedService.diarization_only()      # 最后才做说话人
  → build_caption_rows()                   # 给字幕贴 lid
```

8885 的 v1 适配器最初是为了满足平台合同：固定模型名、业务错误码、整数毫秒时间戳、`max_chars`、`lid`、FIFO、CSV 等。实现时复用了同一个模型对象，却没有复用 `/asr?diarization=true` 已有的“先分段再 ASR”业务流程。这是架构复用层级选择错误，不是 Nginx 或 FastAPI 的限制。

## 三、改造目标

### 3.1 核心目标

1. 8885 `diarize=1` 的识别文字必须来自与 6006 `diarization=true` 相同的核心流程。
2. 6006 原生接口的请求参数、识别方式和响应 JSON 保持不变。
3. 8885 平台接口的请求参数和响应合同保持不变：

   ```json
   {
     "code": 200,
     "language": "zh",
     "data": [
       {"lid": "1", "text": "识别文字。", "begin": 380, "end": 3210}
     ],
     "file_name": "0051409.wav",
     "message": "",
     "uuid": "非空UUID"
   }
   ```

4. 8885 继续通过一个顶层 FIFO 保护整条推理链，不增加模型进程、不增加模型副本、不通过本机 HTTP 再调用 `/asr`。
5. 继续完全离线运行；不增加任何联网回退。
6. 任何对齐失败、文本未完全覆盖或说话人分离失败都应明确报错，不返回看似成功但漏尾的结果。

### 3.2 目标行为矩阵

| 入口 | 参数 | ASR 核心能力 | 对外响应 |
| --- | --- | --- | --- |
| 6006 `/asr` | `diarization=false` | 保持 `asr_raw` | 保持现状 |
| 6006 `/asr` | `diarization=true` | 共享的“先分段再批量 ASR” | 保持现状 |
| 8885 `/v1/audiototext` | `diarize=0` | 与 6006 非分段原生 ASR 一致 | v1 平台合同 + ForcedAligner 时间戳 |
| 8885 `/v1/audiototext` | `diarize=1` | 与 6006 分段 ASR 共用同一核心方法 | v1 平台合同 + 时间戳 + 1 起始 `lid` |

本轮优先解决已经实证的 `diarize=1` 质量差异。`diarize=0` 暂时保持与 6006 `/asr?diarization=false` 相同的整段识别语义，不在同一改动中擅自引入 VAD 策略。

## 四、目标架构

### 4.1 共享 Python 核心，不进行 HTTP 自调用

目标结构：

```text
                          ┌─────────────────────────────┐
6006 /asr?diarization=1 ─┤                             │
                          │  shared diarized ASR core  │
8885 /v1?...&diarize=1  ─┤                             │
                          └──────────────┬──────────────┘
                                         │
                    diarization → clips → batch ASR
                                         │
                         speaker_segments + overall_text
                              ┌───────────┴───────────┐
                              │                       │
                       6006 原响应             8885 v1 适配
                                              ForcedAligner
                                              caption rows
                                              lid 规范化
```

不建议让 `/v1/audiototext` 在服务内部使用 HTTP 请求 `127.0.0.1:6006/asr`，原因是：

- 同一进程内已有 `UnifiedService` 对象，HTTP 会多一次音频复制、解析和序列化；
- v1 外层已有 FIFO，内部 HTTP 容易制造超时、重复排队或故障边界不清；
- 共享 Python 方法才能保证两个入口真正执行同一版本的算法；
- 单元测试可以直接验证调用关系和输出，无需启动服务。

### 4.2 共享结果的内部结构

建议从当前 `diarization_then_asr()` 中提取内部方法，例如：

```text
UnifiedService.transcribe_diarized_segments(
    audio_path,
    input_filename,
    allow_diarization_fallback,
)
```

内部返回至少包含：

```json
{
  "overall_text": "完整拼接文本",
  "speaker_segments": [
    {
      "start": 0.38,
      "end": 3.21,
      "speaker": "0",
      "text": "你好，天台110。"
    }
  ],
  "speaker_count": 2,
  "diarization_status": "ok",
  "detected_languages": ["tiantai"],
  "request_id": "..."
}
```

6006 的 `diarization_then_asr()` 只负责把共享结果包装为现有响应，确保字段、类型和降级行为不变。8885 的适配器读取同一份 `overall_text` 和 `speaker_segments`，再完成平台时间戳与 `lid` 转换。

## 五、详细实施步骤

### 阶段 A：先建立回归测试，锁定现有合同

涉及文件：

- `tailect/tests/test_v1_platform.py`
- `tailect/tests/test_core.py`
- 必要时新增不加载 GPU 的推理编排测试文件

先补测试，再改实现：

1. `diarize=0` 必须调用 `asr_raw()`，不得调用分段 ASR。
2. `diarize=1` 必须调用共享分段 ASR，不能先调用完整音频 `asr_raw()`。
3. 6006 `diarization_then_asr()` 的响应字段和类型必须保持当前形状。
4. 8885 仍返回 `code/language/data/file_name/message/uuid`。
5. 8885 的 `lid` 必须从字符串 `"1"` 开始连续编号，不能直接暴露 6006 的 `0/1`。
6. `max_chars`、标点切分和说话人变化都能触发正确分行。
7. ForcedAligner 时间戳不足以覆盖全文时返回 `E016`，不能静默丢弃尾部文字。
8. diarization 失败且 `v1_diarization_fallback=false` 时保持明确失败。

### 阶段 B：提取共享的“说话人分段 + 批量 ASR”能力

主要涉及：

- `tailect/core/inference_engine.py`

工作内容：

1. 从 `diarization_then_asr()` 提取说话人分离、音频裁剪、批量 ASR、结果组装逻辑。
2. 只保留一个真实实现，避免复制一份代码给 v1。
3. 保留现有 `_asr_lock`、batch 大小限制、KV cache 清理、OOM/张量错误回退和超时处理。
4. 允许调用方指定 diarization 失败策略：

   - 6006 保持现有行为：失败时可退回完整音频 ASR；
   - 8885 遵守当前配置：`v1_diarization_fallback=false` 时返回错误，不能伪装成单说话人。

5. `overall_text` 必须严格由按时间排序的 `speaker_segments[*].text` 拼接，确保 6006 与 8885 的识别文字源相同。
6. 不改变现有模型加载、vLLM 参数、GPU 配置和批量推理方式。

### 阶段 C：保留分段 ASR 的语言信息

主要涉及：

- `tailect/core/inference_engine.py`
- `tailect/core/v1_contract.py`

当前 `transcribe_batch()` 只返回文字，丢弃模型结果中的语言。为维持 8885 的 `language` 字段，建议增加内部详细批量接口：

```text
transcribe_batch_detailed() → [{text, language}, ...]
transcribe_batch()          → 继续返回 [text, ...]
```

兼容策略：

1. 6006 继续使用文字列表，对外响应不增加字段。
2. 8885 从非空片段语言中按文字长度或有效语音时长加权选主语言。
3. 没有模型语言时使用请求参数；`language=auto` 按当前配置回退为 Chinese/`zh`。
4. 继续使用现有 `language_to_v1()` 行为，避免未经确认改变当前已经出现的 `tiantai` 标签。

### 阶段 D：8885 `diarize=1` 改用共享结果

主要涉及：

- `tailect/core/v1_adapter.py`
- 可能少量调整 `tailect/core/v1_router.py`

新流程：

```text
共享分段 ASR
  → 得到与 6006 相同的 overall_text
  → 对完整音频和完整文本调用一次 ForcedAligner
  → 使用 diarization 的 speaker_segments 分配 lid
  → 按说话人变化、标点和 max_chars 聚合平台字幕行
```

第一版继续对完整音频调用一次 ForcedAligner，原因是：

- 保持当前 v1 合同中精细时间戳的来源；
- 不需要为 18 个片段连续调用 18 次 ForcedAligner；
- 新的 `overall_text` 来自已验证较好的分段 ASR，全文与音频的内容覆盖显著改善；
- 推理成本与当前 v1 的“一次 ForcedAligner”接近。

如果生产验证仍发现全局对齐漂移，再实施“逐说话人片段局部 ForcedAligner + 加回全局偏移”的第二级方案；该方案准确度潜力更高，但调用次数、临时音频和超时预算都会明显增加，不应未经实测直接作为唯一方案。

### 阶段 E：修正字幕构建的静默漏尾问题

主要涉及：

- `tailect/core/v1_contract.py`

当前 `build_caption_rows()` 在 ASR 文本还有剩余、但 ForcedAligner 时间戳已经耗尽时，最后可能直接丢弃没有时间戳的文字。需要改为：

1. 跟踪全文中有效字母/数字字符的消费情况。
2. 对齐结果不能覆盖所有有效文本时返回 `E016`。
3. 时间戳必须单调、非负、`end > begin`，并在音频总时长合理范围内。
4. 说话人变化必须是硬分行边界，不能把两个说话人的文字聚合成同一行后只选一个最大重叠 `lid`。
5. 标点和 `max_chars` 继续作为同一说话人内部的软分行条件。
6. 不允许用全零时间戳或人为平均分配时间掩盖对齐失败。

### 阶段 F：保持上传兼容，同时规范测试命令

主要涉及：

- 测试文档
- 可能补充上传解析单元测试

本次根因不是 multipart MIME，因此不应因为客户端写错 `video/mp4` 就改变推理流程。建议：

1. 服务继续以实际 WAV 文件头为准，兼容已有客户端。
2. 日志记录“文件扩展名、声明 MIME、实际格式”不一致告警，但不把客户端本地路径写入公开响应。
3. README 和验收命令统一使用：

   ```powershell
   -F "file=@D:/fanyin/tailect/tests/0051409.wav;type=audio/wav"
   ```

4. 以后若要严格拒绝 MIME 不一致，应作为独立兼容性决策，不能混入本次识别质量修复。

### 阶段 G：补充日志与诊断信息

主要涉及：

- `tailect/core/v1_adapter.py`
- `tailect/core/inference_engine.py`
- `tailect/core/logger.py`（仅在确有必要时）

内部日志增加：

```text
request_id
entrypoint=/asr 或 /v1/audiototext
pipeline=whole_asr 或 diarized_segment_asr
audio_duration / sample_rate / codec / channels
diarization_segment_count / speaker_count
overall_text_length
aligned_token_count / output_row_count
language_candidates / selected_language
```

日志不得包含 API Key，不向客户端返回堆栈或服务器绝对路径。

## 六、测试与验收计划

### 6.1 开发环境静态与单元测试

开发机不加载 GPU 模型，仅执行：

```text
Python 编译检查
tests.test_v1_platform
tests.test_core
油猴/接口静态测试
Web 前端已有测试
```

重点验证编排、响应合同、错误路径和字幕算法；不在开发机伪造真实模型准确率结论。

### 6.2 Ubuntu 生产环境部署前检查

1. 记录 Git 提交号和变更文件清单。
2. 在不启动第二个模型实例的前提下执行无 GPU 单元测试。
3. 确认现有模型容器和平台容器名称只属于本项目。
4. 保存当前容器状态和健康检查结果。
5. 使用 Git 提交作为回滚点；不得删除旧文件或容器。

### 6.3 生产代码加载注意事项

项目目录虽然挂载进 Docker，但 Python 不启用 reload。修改源码后，仅重新运行 `start` 不能保证加载新代码；必须在确认窗口内重启本项目模型进程。

建议使用项目自己的停止/启动脚本，让容器停止后再启动，不删除容器：

```bash
bash wxz/deploy/run_v4_1_single_4090.sh stop
bash wxz/deploy/run_v4_1_single_4090.sh start
```

该操作会短暂中断本项目 6006/8885 服务，实施前需要明确维护窗口。不得影响项目外端口、容器或服务。

### 6.4 Windows 内网 A/B 验收

修复后连续执行同一音频：

```powershell
curl.exe -sS `
  --connect-timeout 10 `
  --max-time 1200 `
  -X POST `
  "http://12.33.114.138:6006/asr?diarization=true" `
  -F "file=@D:/fanyin/tailect/tests/0051409.wav;type=audio/wav" `
  -o D:/fanyin/tailect/tests/test_result_6006_after.json `
  -w "HTTP_STATUS=%{http_code}`n"

curl.exe -sS `
  --connect-timeout 10 `
  --max-time 1200 `
  -X POST `
  "http://12.33.114.138:8885/v1/audiototext?model=Tailect_V4.1&diarize=1&language=auto&max_chars=40" `
  -F "file=@D:/fanyin/tailect/tests/0051409.wav;type=audio/wav" `
  -o D:/fanyin/tailect/tests/test_result_8885_after.json `
  -w "HTTP_STATUS=%{http_code}`n"
```

### 6.5 验收标准

#### 必须通过

1. 6006 原生响应结构与修复前一致。
2. 8885 响应合同不变，`code=200`、UUID 非空、时间戳为整数毫秒。
3. 8885 拼接后的文字来自同一分段 ASR 结果，与 6006 `overall_text` 在忽略平台切行和允许的标点规范后保持一致。
4. 本样本尾部必须包含姓名之后的案件确认、通知派出所和结束语，不得再次在“你叫什么名字”处结束。
5. 不得再出现因漏字导致的 8–17 秒普通短句异常对齐。
6. 两个说话人正确映射为平台 `lid="1"`、`lid="2"`。
7. 8885 FIFO 仍为并发 1，6006/8885 共用一份模型。
8. diarization 或 ForcedAligner 失败时返回明确业务错误，不返回部分成功文本。

#### 需要人工确认

1. “三港站/三角站/三角台”等方言地点词。
2. “水箱弄漏/水枪打漏”等窄带易混词。
3. 姓名“叶海华”等实体。
4. 说话人边界和重叠语音。

建议制作人工逐字稿并计算 CER。8885 与 6006 文字接近只证明流程复用成功，不等于模型达到最终业务准确率。

## 七、风险与控制

| 风险 | 控制措施 |
| --- | --- |
| 重构影响 6006 原生接口 | 先锁定响应测试；6006 只包装共享结果，不改对外合同 |
| v1 耗时增加 | 保留一次批量 ASR和一次全局 ForcedAligner；记录阶段耗时 |
| 全局 ForcedAligner 仍漂移 | 增加全文覆盖校验；必要时再启用逐片段局部对齐 |
| diarization 失败被静默降级 | v1 使用独立 fallback 参数，默认严格失败 |
| batch 语言信息丢失 | 增加详细批量结果，旧 `transcribe_batch()` 签名继续兼容 |
| 说话人字幕跨界 | 说话人变化作为硬分行条件 |
| 源码已挂载但进程未重启 | 部署验收明确重启本项目模型容器 |
| 生产回滚 | 使用 Git 回滚提交并重启本项目容器，不删除文件或容器 |

## 八、预计修改文件

核心修改：

```text
tailect/core/inference_engine.py
tailect/core/v1_adapter.py
tailect/core/v1_contract.py
tailect/tests/test_v1_platform.py
```

视实现需要的小范围修改：

```text
tailect/core/v1_router.py
tailect/tests/test_core.py
spyware-translator-v4.1/tests/tailect_v41_probe.mjs
wxz/migration/acceptance_new4_1.sh
README.md
tailect/README.md
wxz/docs/Tailect_V4.1离线接口与部署.md
```

不计划修改：

```text
模型权重
Docker 基础镜像
CUDA/vLLM 显存参数
8885 对外端口
6006 对外端口
CSV 与人工修正接口
生产环境其它服务
```

## 九、回滚方案

1. 实施前记录当前提交号。
2. 改动形成独立提交，不与 Web 或其它功能改动混合。
3. 验证失败时使用新的 Git 回滚提交恢复旧代码，不删除仓库文件。
4. 只重启本项目的 `tailect-v41-model` 和 `tailect-v41-platform` 容器。
5. 回滚后重新执行 6006、8885 健康检查和原始样本命令。
6. 所有失败结果和日志保留在项目日志目录，供复盘使用。

## 十、实施前默认假设与待确认点

根据用户当前要求，本计划采用以下默认假设：

1. `8885 diarize=1` 必须复用 `6006 diarization=true` 的分段 ASR 能力。
2. `8885 diarize=0` 与 `6006 diarization=false` 保持一致，暂不额外切段。
3. 6006 原生 JSON 不变；8885 v1 JSON 不变。
4. v1 diarization 失败保持严格报错，不采用 6006 的静默整段降级。
5. 第一版使用“分段 ASR 完整文本 + 一次全局 ForcedAligner”；若生产仍发现对齐漂移，再升级为逐片段局部对齐。
6. 当前开发环境不运行 GPU 模型；所有真实准确率和性能由用户协助在 Ubuntu 生产环境验证。

这些假设均可通过现有需求合理确定，当前没有阻塞制定和开始编码的问题。实施到生产重启前，需要用户单独确认维护窗口。

## 十一、R9 本地实施结果

R9 代码、无模型合同测试、油猴端声道合并测试、Linux 迁移脚本、自动同音频验收
脚本和可回滚增量包均已完成。候选包位于：

```text
wxz/migration/packages/fanyin-new4.1-incremental-20260830-r9.tar.gz
SHA-256: 9d4622b1a4bfa207dc9a55addf649d8140c5962d884e62adb71044c5f5de0300
```

剩余工作是 Ubuntu 维护窗口应用、重启本项目、运行同音频自动验收并人工确认方言
实体与时间戳。详细结果见
`wxz/docs/R9_8885复用6006原生转译能力实施与验收.md`。
