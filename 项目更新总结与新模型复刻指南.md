# 项目更新总结与新模型 API、油猴脚本复刻指南

更新时间：2026-08-29  
分析范围：当前工作区、`main` 分支全部 5 个提交、远端 `origin/main`，重点为 `Tailect_ASR_Win10`、`spyware-translator` 及相关接口/部署文档。

## 1. 文档目的

本文不是单纯的提交日志，而是对本项目从“WebUI + 简单油猴脚本”演进为“离线模型 API + 音频采集/合并 + 双端 CSV + 人工修正”的过程进行拆解，目的是让后续拿到新的语音翻译/方言转写模型时，可以按同一套边界复制，而不是重新摸索整条链路。

本文回答四个问题：

1. Git 中每次更新实际增加了什么。
2. 当前模型服务、油猴脚本、本机助手之间如何协作。
3. 更换新模型时哪些模块可以直接复用，哪些位置与 Tailect/Qwen3 ASR 强耦合，必须修改。
4. 新模型完成同样 API 和油猴扩展后，应如何验收，避免“能启动但不可交付”。

## 2. 当前 Git 状态与结论

- 当前分支：`main`
- 当前提交：`bb3701bd893e0ed442b892067064b0f6dab13697`
- 远端：`origin = https://github.com/shiftpop0/fanyin.git`
- 已执行 `git fetch --prune origin`。
- `HEAD` 与 `origin/main` 的领先/落后计数为 `0 / 0`，当前没有遗漏的远端提交。
- 分析开始前工作区干净，没有用户未提交文件需要绕开。

总体结论：

- 最初版本依赖 Gradio 私有调用链，油猴脚本只做单个 WAV 捕获和悬浮展示。
- 2026-06-17 开始形成独立 v1 API：模型一次加载、统一响应、离线自检、Windows 服务管理、日志、鉴权、限流、说话人分离和联调探针。
- 2026-06-23 完成真正面向业务页面的油猴扩展：音频切片探测/合并、业务字段去重、普通列表/VX UI、中文 CSV、双端保存、缓存复用、修正反馈、本机 CSV 助手和便携 Node 离线包。
- 2026-08-29 增加 `file=HTTP(S) URL` 和 `.sdp` 名称兼容，但当前油猴脚本仍采用“浏览器下载并合并切片，再 multipart 上传 WAV”的方式，没有改为把源 URL 直接交给 API。
- 对新模型而言，最稳妥的复刻策略是保持现有 HTTP/CSV/油猴契约不变，只替换模型适配层；这样网页采集、切片合并、CSV、本机助手和 UI 基本无需重写。

## 3. Git 更新演进

| 提交 | 日期 | 主要作用 | 对新模型复刻的意义 |
| --- | --- | --- | --- |
| `e274d89` | 2026-06-15 | 初始项目：WebUI 启动器、修改后的 `qwen_asr/cli/demo.py`、油猴脚本 0.1.0 | 提供最初的音频捕获、Gradio 上传/队列/SSE 调用和悬浮展示原型 |
| `eafea6c` | 2026-06-15 | 轻量 Git 上传、流式/服务入口、mock、离线更新包和 Gradio HTTP 探针 | 验证离线更新和测试页面，但仍以 Gradio 链路为主 |
| `490f4c5` | 2026-06-17 | 新增独立 v1 API、preflight、配置化启停、联调文档、v1 probe 和实施记录 | 建立可复用的“模型适配器 + 稳定 HTTP 契约”主体 |
| `d3b7ec2` | 2026-06-23 | 大幅扩展 API、Windows 服务、油猴脚本；新增本机 CSV 助手、离线客户端打包 | 建立可交付的网页端完整业务闭环，是新模型复刻的主要模板 |
| `bb3701b` | 2026-08-29 | `file` 字段支持 HTTP(S) URL，增加 `.sdp` WAV 别名、下载安全控制和文档 | 新模型 API 必须继续保留的最新输入兼容层 |

### 3.1 初始阶段：Gradio WebUI 与油猴原型

初始油猴脚本版本为 `0.1.0`，默认连接 `http://127.0.0.1:7867/gradio_api`。主要链路为：

1. Hook 页面 `fetch` 和 `XMLHttpRequest`，同时扫描 DOM、Performance Resource。
2. 捕获 `/spyfile/audiostream.wav?...`。
3. 下载单个音频。
4. 调用 Gradio 上传接口。
5. 调用 Gradio 队列接口并读取 SSE。
6. 解析 SRT 或纯文本后显示在悬浮面板。

这一版本说明网页音频可以被油猴脚本捕获，但不适合直接复刻到新模型，原因是 Gradio 的内部接口、队列事件和输出格式都不是稳定业务契约。

### 3.2 2026-06-17：建立独立 v1 API

`490f4c5` 新增的核心文件包括：

- `Tailect_ASR_Win10/WPy64-312101/python/Lib/site-packages/tailect_asr/cli/api_v1.py`
- `Tailect_ASR_Win10/WPy64-312101/python/Lib/site-packages/tailect_asr/cli/preflight.py`
- `Tailect_ASR_Win10/api_v1_config.json`
- `Tailect_ASR_Win10/api_v1_service.ps1`
- `Tailect_ASR_Win10/API服务-启动.bat`
- `Tailect_ASR_Win10/API服务-停止.bat`
- `Tailect_ASR_Win10/API服务-状态.bat`
- `Tailect_ASR_Win10/一键启动API服务.bat`
- `Tailect_ASR_Win10/一键模型自检.bat`
- `spyware-translator/temp/tailect_v1_probe.mjs`

这一阶段完成的关键设计：

- 模型在服务启动时加载一次，不再每个请求重复加载。
- 对外固定为 `POST /v1/audiototext`。
- 对外响应固定为 `code/language/data/file_name/message/uuid`。
- 字幕行固定为 `lid/text/begin/end`，时间单位为毫秒。
- 支持 Flask 和 FastAPI/uvicorn，两条实现保持同一接口。
- 使用进程内锁串行执行大模型推理，请求可在 `queue_timeout_sec` 内等待。
- 支持上传大小限制、API Key、单客户端限流、日志轮转和统一错误码。
- 非 WAV 文件由离线 `ffmpeg` 转成标准 WAV。
- 将 Qwen3 ASR 的全文、时间戳和 ModelScope 说话人分离结果映射为 v1 字幕行。
- 离线启动时设置 ModelScope/Hugging Face/Transformers 离线环境，关闭代理并抑制 FunASR 在线更新检查。
- preflight 先检查模型结构、ForcedAligner、说话人模型，再做真实加载和样本推理。

### 3.3 2026-06-23：业务闭环与离线交付

`d3b7ec2` 是整个项目变更量最大、最值得复刻的一次更新，主要由五部分组成。

#### A. Windows 服务可靠性

- `API服务-启动.bat`、`停止.bat`、`状态.bat` 统一调用 `api_v1_service.ps1`。
- 启动后轮询 `/health`，只有 `status=ok` 才报告成功，最长等待 180 秒。
- 端口若被非 Tailect 进程占用，拒绝启动。
- 端口若已是健康的 Tailect API，复用进程并刷新 PID 文件。
- stdout/stderr 分文件写入 `outputs/logs`。
- 停止后清空 PID，状态同时检查 PID、端口监听者和 `/health`。
- BAT 统一传递真实退出码，修复 LF 换行对 Windows CMD 的影响。
- WebUI/API/preflight 启动器都能传入模型名，交互式启动器会枚举 `models\ASR` 下的目录。

#### B. API 的 CSV 与反馈能力

模型端新增：

- `GET /translator/csv/status`
- `GET /translator/csv`
- `POST /translator/csv`
- `POST /translator/feedback`

模型端默认把 CSV 保存到：

```text
C:\fanyin_output\{client_ip}\{csv_filename}
```

同步状态保存到：

```text
C:\fanyin_output\.state\sync_state.sqlite
```

SQLite 记录 `client_ip`、`record_key`、文件名、修订号、版本 ID、内容哈希、写入事件等信息。当前策略不是做冲突合并，而是“脚本端最新一次提交覆盖模型端”，适合本项目的单人修正流程。

模型 API 如果误收到本应发给浏览器电脑本机助手的 `/local/csv/save` 或 `/local/csv/open-path`，会返回 `LOCAL_HELPER_REQUIRED`，提示正确端口为 `127.0.0.1:18885`。

#### C. 油猴脚本从 Gradio 切换到 v1

当前油猴脚本版本为 `0.4.4`，默认模型 API 为 `http://127.0.0.1:8885`，核心调用为：

```text
POST /v1/audiototext
  ?model=<模型名>
  &diarize=0|1
  &language=auto|zh|...
  &max_chars=40
multipart field: file=<合并后的 WAV>
```

脚本主要能力：

- Hook `fetch`、XHR、DOM 音频节点和 Performance Resource 捕获音频。
- 捕获 `/spyfile/audiostream.wav?targetfile=...&index=...`。
- 从 `index=0` 向后探测切片，默认没有业务上限，连续失败默认 2 次后停止。
- 合并 PCM WAV；超过 10 分钟时拆成多个识别 part，并为每个 part 的时间戳增加偏移。
- 队列串行识别，避免浏览器端同时压满模型服务。
- 把 API 返回的 `lid` 按首次出现顺序重新规范为 `1,2,3...`。
- 生成 25 列中文 CSV。
- 先查本机中文 CSV 缓存，命中后跳过自动识别。
- 识别结果同时保存到浏览器电脑和模型电脑。
- 展开弹窗可播放音频、查看带时间戳文本、修正单行内容、重新识别、CSV 另存和打开文件路径。
- feedback 历史保留但默认关闭；修正时仍会覆盖双端 CSV。
- 控制台配置写入 `localStorage`，包括模型地址、模型名、API Key、本机助手、切片设置、列宽和面板位置。

#### D. 业务页面适配

普通列表：

- 在 Wijmo FlexGrid 的“序号”右侧插入“识别内容”列。
- 通过 Wijmo 列绑定、表头文字和原始 `left` 坐标映射业务字段，不依赖固定 DOM 下标。
- 业务键/标题/CSV 文件名统一使用：

```text
案件名称 + 侦控号码 + 对方号码 + 通话开始时间 + 预估时长（秒） + index=0
```

- 底层 `targetfile` 不参与普通通话业务去重；同一通话每次产生不同临时路径时仍合并为一个任务。
- 未捕获音频时显示“等待点开音频”，进入 60 秒等待，要求用户手动点开语音；脚本不再自动操作未知业务 DOM。
- 虚拟滚动下只调整 FlexGrid 的 cells/chcells/sz 内容层，避免把整个视口扩宽。

VX 页面：

- 业务键使用“VX + 联系人 + 收发方向 + 消息时间 + 语音时长 + index=0”。
- 手动识别/重新识别和展开按钮挂在最外层音频横向容器右侧。

通用音频页面：

- 对带 `audioBox` 特征的音频增加同类入口，无法取得完整业务字段时使用通用标题。

#### E. 本机 CSV 助手和离线客户端

浏览器安全模型不允许油猴脚本静默写任意 Windows 路径，因此增加 Node 本机助手：

- 默认监听：`http://127.0.0.1:18885`
- 默认输出：`C:\fanyin_output`
- 服务身份：`fanyin-local-csv-helper`
- 支持健康检查、本机 IP、CSV 状态/读取/保存、打开 Explorer、feedback。
- `/local/...` 为正式路由，同时兼容 `/csv/...` 旧路由。
- “CSV另存”不依赖助手；优先 `showSaveFilePicker`，不支持时回退浏览器下载。
- “打开文件路径”依赖助手，用 Explorer `/select,"<完整路径>"` 选中文件。
- `build_offline_client.ps1` 把 userscript、助手、启动 BAT、README 和 Windows x64 `node.exe` 打成目录及 ZIP，并写入 Node 版本、架构和 SHA-256。
- 正式离线包要求 Node.js 18+ x64；便携运行时不提交 Git。

### 3.4 2026-08-29：URL 与 SDP 兼容

`bb3701b` 对 API 的输入层做了以下扩展：

1. `file` 可以继续是 multipart 上传文件，也可以是 query/form 中的 HTTP(S) URL。
2. URL 下载在后台线程执行，避免阻塞 FastAPI 事件循环。
3. URL 下载继续受 `max_upload_mb` 限制。
4. URL 只允许 `http/https`，禁止 URL 内用户名和密码。
5. 支持主机、`*.example.com`、`.example.com`、IP 和 CIDR 白名单。
6. 每次重定向重新验证目标，默认最多 5 次。
7. 默认下载超时 30 秒。
8. 禁用代理下载，避免意外继承机器代理。
9. 下载结果必须通过 RIFF/RF64/BW64 + WAVE 文件头检查，否则返回 `E015`。
10. 上传文件扩展名为 `.sdp` 时，只在内容确实是 WAV 的情况下改名为 `.wav` 再识别。
11. URL 的 `filename1`、`filename`、`file_name` 查询参数可作为响应业务文件名；`.sdp` 名称不用于判断媒体内容。

新增参数：

- `--audio-url-timeout-sec`，默认 30 秒。
- `--audio-url-max-redirects`，默认 5。
- `--audio-url-allow-hosts`，也可由 `TAILECT_AUDIO_URL_ALLOW_HOSTS` 提供。

新增错误码：

- `E013`：URL 无效、协议不支持、带凭据或主机不允许。
- `E014`：HTTP 错误、下载失败、超时或重定向过多。
- `E015`：下载内容不是有效 WAV。

需要特别注意：

- `.sdp` 在本项目里不是 SDP 协议解析器，只是“内容为 WAV、名字以 `.sdp` 结尾”的兼容别名。
- URL 模式只接受一个可直接下载的完整 WAV。当前业务音频是 `index=0..N` 切片，仍需要油猴脚本探测、下载和合并，不能仅把 `index=0` URL 交给 API 后期待服务自动抓取全部切片。

## 4. 当前总体架构

```text
侦控网页
  │  fetch / XHR / DOM / Performance 捕获
  ▼
Tampermonkey userscript
  │  业务字段提取、去重、切片探测、WAV 合并/分段
  ├──────── multipart WAV ────────► 模型端 :8885 /v1/audiototext
  │                                  │
  │                                  ├─ Qwen3 ASR / 新模型适配器
  │                                  ├─ ForcedAligner 时间戳
  │                                  └─ ModelScope 说话人分离
  │
  ├──────── 中文 CSV ─────────────► 模型端 /translator/csv
  │                                  └─ C:\fanyin_output\{client_ip}
  │
  └──────── 中文 CSV ─────────────► 浏览器电脑 :18885 本机助手
                                     └─ C:\fanyin_output
```

这个架构的复制边界非常清楚：网页侧只依赖稳定 v1 契约，不应该知道新模型是 Qwen、Whisper、FunASR 还是其它框架；模型差异应收口在模型端适配层。

## 5. 必须保持稳定的接口契约

### 5.1 健康检查

```http
GET /health
```

油猴脚本当前只要求返回对象中 `status=ok` 或 `code=200`。推荐新模型继续返回至少：

```json
{
  "status": "ok",
  "model": "NewModel",
  "server": "uvicorn",
  "cuda": true
}
```

建议新扩展同时校验 `health.model === settings.model`；当前脚本没有做这项检查，可能出现“健康检查正常，但请求模型与服务实际加载模型不同，随后返回 E009”。

### 5.2 转写接口

```http
POST /v1/audiototext?model=<alias>&diarize=0|1&language=auto&max_chars=40
```

输入二选一：

- `multipart/form-data` 的 `file` 上传音频。
- query 或 URL-encoded form 的 `file=<HTTP(S) URL>`。

成功和业务失败都保持 HTTP 200，由 body 的 `code` 区分：

```json
{
  "code": 200,
  "language": "zh",
  "data": [
    {
      "lid": "1",
      "text": "识别文本",
      "begin": 1280,
      "end": 2240
    }
  ],
  "file_name": "audio.wav",
  "message": "",
  "uuid": "任务 UUID"
}
```

新模型适配层必须保证：

- `code` 为整数。
- `data` 始终为数组。
- `lid` 和 `text` 为字符串。
- `begin/end` 为整数毫秒。
- `uuid` 每次请求都有非空值。
- 失败时仍返回完整顶层字段，`code=500`，`message` 带可定位错误信息。

### 5.3 模型端 CSV 与修正接口

油猴脚本还依赖：

- `GET /translator/csv/status`
- `GET /translator/csv`
- `POST /translator/csv`
- `POST /translator/feedback`

如果新模型仍使用当前 `api_v1.py` 外壳，这些接口可原样保留。若另起服务，不能只实现 `/v1/audiototext`，否则识别完成后会在“保存 CSV”阶段报错，整个任务最终表现为失败。

### 5.4 本机助手接口

本机助手与模型无关，可直接复用：

- `GET /local/health`
- `GET /local/ip`
- `GET /local/csv/status`
- `GET /local/csv`
- `POST /local/csv/save`
- `POST /local/csv/open-path`
- `POST /local/feedback`

## 6. 模型相关与模型无关代码边界

| 模块/文件 | 与模型的耦合度 | 新模型处理方式 |
| --- | --- | --- |
| `TailectApiService.load()` | 高 | 同框架改 checkpoint；不同框架重写加载器 |
| `TailectApiService.transcribe_file()` | 高 | 把新模型输出适配为“语言 + 带时间戳文本 + 全文” |
| `ASR_REQUIRED_FILES` | 高 | 按新模型真实目录结构修改，不能沿用 Qwen 文件清单 |
| `ALLOWED_MODEL_ALIASES`、`canonical_model_alias()` | 高 | 新增公开模型别名及大小写规范化 |
| `LANGUAGE_TO_V1`、`V1_TO_LANGUAGE` | 中 | 新模型语种名称不同则增加映射 |
| ForcedAligner | 中/高 | 新模型若自带可靠时间戳可替换；否则继续依赖 |
| ModelScope 说话人分离 | 低 | 与 ASR 主模型相对独立，可继续复用 |
| `build_caption_rows()` | 中 | 输出仍为全文+词级时间戳时可复用；输出结构不同则适配 |
| 上传、URL/SDP、ffmpeg、错误响应 | 低 | 原样复用 |
| API Key、限流、队列锁、日志、CORS | 低 | 原样复用，但应复核安全配置 |
| `/translator/*`、SQLite、CSV 落盘 | 低 | 原样复用 |
| Windows 启停/状态脚本 | 低/中 | 入口模块和运行时不变时原样复用；否则替换 Python/命令 |
| 油猴音频捕获、切片合并、页面 UI | 低 | v1 契约不变时原样复用 |
| 油猴默认模型名、缓存策略 | 中 | 必须迁移，不能只改 API 服务 |
| 本机 CSV 助手、便携 Node 打包 | 无 | 原样复用 |

## 7. 新模型复刻实施步骤

### 阶段 0：先判断模型兼容类型

实施前必须确认以下信息：

- 新模型是否仍可由 `Qwen3ASRModel.from_pretrained()` 加载。
- 是否继续需要同一个 ForcedAligner。
- 模型是否返回词/字级时间戳；时间单位是秒、毫秒还是采样点。
- 模型语言字段返回什么值。
- 是否支持长音频，单次安全时长和显存峰值是多少。
- 输入要求是否固定为 16kHz、单声道、PCM WAV。
- 是否需要额外 Python 包、CUDA/torch 版本或自定义代码。
- 是否保留现有说话人分离。
- 公开模型名是什么，是否要与旧模型并存。

根据答案选择两条路径之一。

#### 路径 A：新模型与当前 Qwen3 ASR 运行时兼容

这是改动最小的路径：

1. 将模型目录放到 `Tailect_ASR_Win10/models/ASR/<NewAlias>`。
2. 确认目录包含当前启动检查所需文件，或按实际情况调整 `ASR_REQUIRED_FILES`。
3. 在 `ALLOWED_MODEL_ALIASES` 增加 `<NewAlias>`。
4. 在 `canonical_model_alias()` 增加别名/大小写规范化，或者把该函数改为由显式配置驱动。
5. 修改 `api_v1_config.json` 的 `model`。
6. 用 `一键模型自检.bat <NewAlias>` 做加载、时间戳和说话人链路检查。
7. 用独立端口启动服务，避免与旧模型进程混淆。
8. 修改油猴默认模型并处理旧配置/旧缓存迁移。

注意：`一键启动API服务.bat` 会枚举目录，因此新目录能出现在菜单里；但 `api_v1.py` 仍会通过硬编码公开别名检查拒绝未知模型。菜单可见不等于 API 可用。

#### 路径 B：新模型使用不同推理框架

建议保留 `api_v1.py` 的 HTTP 外壳、输入处理、CSV、鉴权、日志和 Windows 服务，只把以下模型适配点抽象/替换：

```python
class ModelAdapter:
    def load(self) -> None:
        ...

    def transcribe(
        self,
        audio_path: Path,
        language: Optional[str],
    ) -> tuple[str, list[dict], str]:
        """返回 internal_language、timestamp items、full_text。"""
```

推荐先把新模型输出统一为：

```python
[
    {"text": "第一段", "start_time": 1.28, "end_time": 2.24},
    {"text": "第二段", "start_time": 2.24, "end_time": 3.80},
]
```

再复用 `build_caption_rows()`、说话人重叠匹配和 v1 响应生成。这样不会让模型 SDK 的私有返回对象扩散到路由和油猴脚本。

如果新模型没有时间戳，现有代码会退化为一行 `begin=0/end=0`。这虽然符合字段结构，但会明显降低 CSV、字幕和人工修正体验，应在模型选型阶段明确是否可接受，不能在交付时临时决定。

### 阶段 1：复制并调整模型端

建议按以下顺序操作：

1. 复制当前 `tailect_asr` 服务代码到新模型对应包，或先在现有包中增加适配器。
2. 修改公开模型别名、checkpoint 解析和模型必需文件检查。
3. 修改 `load()`，确保模型只加载一次。
4. 修改 `transcribe_file()`，保持进程内锁、排队超时和异常释放。
5. 统一新模型语言值到 `zh/en/ja/ko/yue`。
6. 将时间戳统一为秒后交给字幕构造，最终响应再输出整数毫秒。
7. 明确说话人分离在 ASR 前还是后运行；当前实现先运行说话人分离，再运行 ASR，最后按时间重叠分配 `lid`。
8. 保留上传、URL/SDP、ffmpeg、鉴权、限流、日志和 `/translator/*`。
9. 配置单进程单模型运行；不要直接启用多 worker，否则每个 worker 都会占一份显存。
10. 为新运行时更新离线环境、依赖和 Git 跟踪白名单。

### 阶段 2：复制并调整 Windows 运行入口

需要同步检查：

- `api_v1_config.json`：模型名、设备、dtype、batch、token、说话人开关、端口。
- `api_v1_service.ps1`：Python 位置、模块入口、离线变量、模型/说话人模型缓存路径。
- `一键启动API服务.bat`：入口模块、模型根目录、帮助信息。
- `一键模型自检.bat`：新模型名、preflight 参数、是否强制说话人分离。
- `一键启动WebUI.bat`：如果新模型不再提供 WebUI，可保留旧模型专用，不要让它误导为新 API 的必要依赖。
- `.gitignore`：只提交最小源码和脚本，不提交模型、运行时、日志、上传音频和输出 CSV。

如果旧模型与新模型要同时在线，当前架构是一进程一模型，应使用不同端口、不同 PID/日志标识，或者先改造服务管理脚本支持实例名。不要让两个配置共用同一个固定 `outputs/api_runtime/api_v1.pid`。

### 阶段 3：油猴脚本迁移

如果新模型保持 v1 契约，网页捕获、切片、UI、CSV 和本机助手无需重写，至少要做以下修改：

1. 提升 userscript 的 `@version`。
2. 修改 `@description` 中的模型说明。
3. 修改 `DEFAULT_SETTINGS.modelBaseUrl`（如果端口/主机变化）。
4. 修改 `DEFAULT_SETTINGS.model`。
5. 决定是否保留 `@connect *`；正式部署更建议列出模型服务、本机助手和业务站点。
6. 更新控制台可见文案，避免仍显示 Tailect。
7. 在模型健康检查中增加 `health.model` 与当前设置模型名的一致性验证。
8. 更新 CSV 的“识别模型”字段，确认修正和另存时不会覆盖成旧模型名。
9. 重新构建便携 Node 离线客户端包，并确认包内 userscript 是新版本。

#### 必须处理 localStorage 迁移

当前配置使用：

```text
STORAGE_KEY = tailect_asr_translator_v2_settings
SETTINGS_SCHEMA_VERSION = 3
```

加载顺序是：

```text
默认设置 < localStorage 已保存设置 < window.__TAILECT_ASR_TRANSLATOR_CONFIG__
```

因此，仅修改 `DEFAULT_SETTINGS.model`，已经安装过旧脚本的浏览器仍会继续使用 localStorage 中的 `Taizhou`。可选方案：

- 为新模型使用新的 `STORAGE_KEY`，完全隔离旧配置；或
- 提高 schema version，在迁移逻辑中把明确的旧默认模型改为新模型；或
- 由部署页面设置 `window.__TAILECT_ASR_TRANSLATOR_CONFIG__` 强制覆盖；或
- 要求现场手动在面板中修改并保存。

推荐：如果新旧模型并存或需要对比，使用新的存储命名空间；如果新模型完全替代旧模型，做一次有条件 schema 迁移，保留用户的地址、列宽和面板位置。

这是实施时的重大产品选择，应由模型是否并存决定，不应直接硬编码。

#### 必须处理旧 CSV 缓存污染

当前普通任务的业务键和 CSV 文件名不包含模型名，本机缓存判断只要求 CSV 是中文表头，没有校验 CSV 中“识别模型”是否等于当前模型。结果是：

- 换成新模型后，同一通话若已经存在旧模型 CSV，自动识别可能直接读取旧缓存，根本不会调用新模型。
- 新模型重新识别后还可能覆盖同名旧模型 CSV，无法做效果对比。

可选方案：

- 推荐用于并存/对比：在缓存目录或 CSV 文件名中增加模型命名空间。
- 推荐用于完全替换：读取缓存时校验“识别模型”，不一致则视为缓存失效并重新识别。
- 临时现场方案：清理或迁移旧 CSV 后再部署，但可重复性和可审计性较差。

这一点必须在开始改油猴脚本前确认，否则测试结果可能看起来正常，实际展示的是旧模型结果。

### 阶段 4：是否使用 URL 直传

当前油猴脚本的标准链路应继续保留：

```text
抓取 index=0..N → 浏览器下载切片 → 合并 WAV → multipart 上传 API
```

只有在上游提供“单个 URL 即完整 WAV”时，才适合把 `file=<URL>` 直接提交给 API。若决定增加 URL 直传模式，还需确认：

- API 模型电脑能否访问该业务 URL。
- URL 是否依赖浏览器 Cookie、Authorization、Referer 或内网会话；当前 API 下载器不会复制浏览器登录态。
- URL 是否真的包含完整音频，而不是单个切片。
- `TAILECT_AUDIO_URL_ALLOW_HOSTS` 是否只允许业务音频主机。
- URL 到期时间是否覆盖模型排队和下载时间。

未经这些确认，不应把现有切片合并链路改成 URL 直传。

## 8. 新模型验收清单

### 8.1 静态和配置检查

- [ ] Python 源码可 `py_compile`。
- [ ] userscript、本机助手、mock、probe 可通过 `node --check`。
- [ ] 新模型目录没有被意外提交 Git。
- [ ] 新增 Python 依赖已进入离线运行时。
- [ ] `ALLOWED_MODEL_ALIASES`、配置文件、启动器、preflight、probe、userscript 的模型名一致。
- [ ] URL 下载超时、重定向和白名单有明确配置。
- [ ] API Key 仅通过环境变量/现场配置提供，没有写入 Git 或 userscript 发行包。

### 8.2 模型自检

- [ ] 模型必需文件检查通过。
- [ ] CUDA、dtype、device 配置符合新模型要求。
- [ ] 模型能离线加载，不发生联网下载。
- [ ] 样本能返回文本。
- [ ] 时间戳单调、单位正确、`begin <= end`。
- [ ] 说话人分离开启和关闭均符合预期。
- [ ] 显存峰值和单次耗时有记录。

### 8.3 v1 合同测试

- [ ] `/health` 返回 `status=ok` 和正确模型名。
- [ ] 缺少 `model` 返回 `E001`。
- [ ] 不支持模型返回 `E002`。
- [ ] 缺少 `file` 返回 `E010`。
- [ ] 空文件返回 `E007`。
- [ ] 正常 WAV 上传返回完整顶层字段。
- [ ] 非 WAV 上传能通过 ffmpeg 转换，或返回明确错误。
- [ ] `.sdp` 名称且内容为 WAV 时成功。
- [ ] `.sdp` 名称但内容非 WAV 时返回 `E004`。
- [ ] 完整 WAV URL 成功。
- [ ] 非 HTTP(S)、带凭据、非白名单 URL 返回 `E013`。
- [ ] HTTP 错误、超时和重定向过多返回 `E014`。
- [ ] URL 返回 HTML/JSON/非 WAV 时返回 `E015`。
- [ ] 超大上传和超大 URL 响应返回 `E003`。
- [ ] API Key、Bearer 和错误密钥行为正确。
- [ ] 限流和排队超时可观察。

### 8.4 油猴端回归

- [ ] 页面 fetch/XHR/DOM/Performance 四种捕获路径正常。
- [ ] 音频切片从 0 开始探测，连续失败阈值生效。
- [ ] 多切片合并后音频可播放。
- [ ] 超过 10 分钟分 part 后，时间戳偏移正确。
- [ ] 普通列表业务字段未错位。
- [ ] 同一业务记录不同 `targetfile` 不重复建任务。
- [ ] “等待点开音频”60 秒流程正常。
- [ ] VX/通用音频按钮位置正常。
- [ ] 新模型不会读取旧模型 CSV 缓存。
- [ ] 说话人编号连续为 `1,2,3...`。
- [ ] 重新识别覆盖策略符合产品决定。
- [ ] CSV 另存、本机保存、模型端保存互不错误耦合。
- [ ] 本机助手停止时，模型端识别和“CSV另存”仍可工作。
- [ ] 修正提交同步更新双端 CSV。

### 8.5 两台电脑与离线交付

- [ ] 模型电脑运行 `:8885`，浏览器电脑运行 `127.0.0.1:18885`。
- [ ] 浏览器配置的模型服务地址指向模型电脑真实 IP，而本机助手仍为浏览器电脑 `127.0.0.1`。
- [ ] 模型端 CSV 落到正确 `{client_ip}` 目录。
- [ ] 断网后模型、说话人分离、本机助手和油猴均可运行。
- [ ] 离线包包含正确 userscript 和完整便携 Node。
- [ ] 包内 Node 版本、架构、SHA-256 与清单一致。

## 9. 当前仓库中已发现的缺口与风险

以下内容是本次阅读代码和 Git 历史后发现的现状，不代表已修改。

### 9.1 阻断项：preflight 未同步 8 月新增参数

`bb3701b` 在 `build_runtime_config()` 中开始读取：

- `args.audio_url_timeout_sec`
- `args.audio_url_max_redirects`
- `args.audio_url_allow_hosts`

但 `preflight.py` 手工构造的 `argparse.Namespace` 没有这三个属性。因此当前 `一键模型自检.bat` 进入 `build_runtime_config()` 时可能抛出 `AttributeError`，发生在模型真实加载之前。

复刻前应做其中一种修复：

- 在 preflight 参数解析器和 Namespace 中同步这三个字段；或
- 让 `build_runtime_config()` 使用 `getattr(args, ..., default)`，为旧调用方保留默认值。

修复后必须真实运行一次 preflight，不能只做 `py_compile`，因为该问题属于运行时参数缺失，静态编译不会发现。

### 9.2 新 URL 配置未写入默认 JSON

`api_v1_config.json` 当前没有：

- `audio_url_timeout_sec`
- `audio_url_max_redirects`
- `audio_url_allow_hosts`

服务会使用命令行默认值，主机规则还可以通过 `TAILECT_AUDIO_URL_ALLOW_HOSTS` 提供，所以不是启动阻断；但为了可复制和可审计，建议新模型配置文件显式列出这三项。

### 9.3 URL 白名单默认空，等价于允许任意 HTTP(S) 主机

当前逻辑中 allowlist 为空表示 unrestricted。若服务对局域网其它主机开放，调用方可以让模型服务器访问任意 HTTP(S) 地址，存在 SSRF/内网探测风险。生产部署应显式设置业务音频主机、IP 或 CIDR，并继续保持每次重定向重新校验。

### 9.4 文档存在版本分叉

- `README.md` 更新时间仍为 2026-06-23，没有记录 8 月 URL/SDP 更新。
- `API_v1_离线部署与联调手册.md` 仍只描述 multipart 上传和 E001-E012。
- 新增的 `API_v1_tz.md` 描述 URL/SDP 和 E013-E015，但示例写入了固定现场 IP `12.33.114.183`。
- `API_v1_tz.md` 的健康示例显示 `auth_enabled=true`，调用示例却没有 API Key；直接复制到启用鉴权的环境会失败。

新模型交付时应合并为一份主手册，以变量 `<MODEL_HOST>`、`<PORT>`、`<MODEL_ALIAS>`、`<API_KEY>` 表示现场值。

### 9.5 v1 probe 未覆盖 URL/SDP

现有 `tailect_v1_probe.mjs` 覆盖健康检查、缺参、非法模型、空文件和正常 multipart 上传，但没有覆盖：

- URL 成功下载。
- URL 白名单。
- 超时/重定向。
- URL 非 WAV。
- `.sdp` 名称的有效/无效内容。

新模型验收前应补齐，否则 8 月更新只能靠手工 curl 验证。

### 9.6 油猴脚本的健康检查不校验模型名

当前只判断服务是否健康，不判断该进程加载的模型是否等于控制台配置模型。建议迁移时增加一致性检查，并在错误提示中同时显示“当前设置模型”和“服务已加载模型”。

### 9.7 油猴缓存未按模型隔离

当前缓存键/CSV 文件名不含模型，读取缓存也不检查“识别模型”列。这是新模型迁移最容易造成假测试通过的问题，必须按第 7 节先确定“并存隔离”还是“替换失效”策略。

### 9.8 上传与转码文件不会自动清理

当前文档明确说明 `outputs/api_uploads/<uuid>/` 不会自动删除上传和转码文件。长期运行会持续占用磁盘。新模型如果进入生产常驻，应增加保留期或安全清理机制，并确保清理目标严格限定在 API upload 目录。

### 9.9 当前公开别名和模型文件结构仍为硬编码

即使启动器能发现任意模型目录，API 只允许 `Taizhou/Tiantai`，preflight 也要求 Qwen/ForcedAligner 的固定文件清单。若后续模型会持续增加，建议把“模型别名、路径、适配器类型、必需文件、默认语言、是否需要 aligner”改为注册表/配置，而不是每来一个模型改多处分支。

## 10. 推荐的最小交付文件集合

新模型复刻完成后，至少应交付：

### 模型端

- v1 API 服务代码及新模型适配器。
- preflight/模型自检。
- API 配置 JSON。
- Windows 启动、停止、状态和交互式启动脚本。
- 离线 Python 运行时和依赖说明。
- 模型、aligner、说话人模型的离线放置说明。
- v1 合同探针和 URL/SDP 扩展测试。
- 统一部署联调手册。

### 浏览器端

- 提升版本后的 userscript。
- 本机 CSV 助手。
- 本机助手启动 BAT。
- Windows x64 便携 Node。
- 离线包信息、版本、架构、哈希。

### 不进入 Git 的内容

- 模型权重。
- WinPython/Node 大运行时，除非专门发布离线二进制包。
- `outputs`、API 上传文件、日志、CSV、SQLite。
- 临时 mock 输出和打包目录。
- WAV/MP3/MP4/SRT/ZIP/EXE/DLL/模型权重等大文件。

## 11. 建议实施顺序

1. 先确认新模型属于“同 Qwen3 运行时”还是“不同框架”。
2. 先修复 preflight 新参数缺失，使现有基线自检恢复可用。
3. 在模型端实现新适配器并通过真实音频自检。
4. 保持 v1 和 `/translator/*` 契约不变，通过合同探针。
5. 决定新旧模型是并存还是替换，据此确定端口/PID、localStorage 和 CSV 缓存策略。
6. 再改油猴默认模型、健康校验、版本号和文案。
7. 使用 mock 做列表、VX、切片、CSV、反馈回归。
8. 做两台电脑、断网、API Key、URL 白名单和磁盘输出验收。
9. 最后生成便携 Node 离线包并核验包内容。

如果按这个顺序执行，新模型改造的核心工作会集中在模型加载、推理输出规范化和迁移策略三个位置；网页采集、切片处理、CSV 闭环和离线助手可以最大程度直接复用。

