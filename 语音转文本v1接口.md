

## 语音转文本接口v1

==功能：语音识别，支持中英日韩粤语识别，支持标点符号输出，支持时间戳输出==

### 1、url

```shell
POST http://192.168.71.110:8885/v1/audiototext
```

### 2、请求参数

| 参数  | 类型       | 是否必填 | 说明     | 提交数据方式                                |
| ----- | ---------- | -------- | -------- | ------------------------------------------- |
| model | string     | 是       | 模型名称 | 默认表单(application/x-www-form-urlencoded) |
| file  | audio file | 是       | 语音文件 | multipart/form-data                         |

例子：

```shell
curl -X 'POST' \
  'http://192.168.71.110:8885/v1/audiototext?model=SenseVoiceSmall' \
  -H 'accept: application/json' \
  -H 'Content-Type: multipart/form-data' \
  -F 'file=@zh.wav;type=audio/wav'
```

### 3、返回值

#### **根据response 的status为200请求成功， 返回结果response 的body的格式如下：**

| 参数      | 类型   | 说明                                                         |
| --------- | ------ | ------------------------------------------------------------ |
| code      | int    | 服务返回代码值，200：成功，500：失败（和response的status不一样） |
| language  | string | 语种（zh: 中文, en: 英语, ja: 日语, ko: 韩语, yue: 粤语）    |
| data      | array  | 转写内容                                                     |
| file_name | string | 传入语音文件名                                               |
| message   | string | 返回code:500的失败错误信息                                   |
| uuid      | string | uuid 编号                                                    |

#### data，结构参数如下：

| 参数  | 类型   | 说明             |
| ----- | ------ | ---------------- |
| lid   | string | 说话人序列号     |
| text  | string | 文本             |
| begin | int    | 起始时间（毫秒） |
| end   | int    | 结束时间（毫秒） |



#### v1例子：

```json
{
    "code": 200,
    "language": "zh",
    "data": [
        {
            "lid": "1",
            "text": "嗯，那么今天我们就简单的进行一下那个新生招聘的嗯讨论吧。因为现在不是马上就新生到校嘛，然后我们社团呢也需要招聘一些新的社员。然后就今天就大概就讨论一下嗯，怎么招聘的内容吧。嗯我们就首先想一下那个招聘的地点在哪里吧。嗯，地点的话，我们现在可以有三个选择。嗯，第一个的话我们可以选择在操场，因为那儿。嗯，学生流动量也挺大的。操场的话，这这段时间太热了，我怕那个人流量有点少。嗯，那我们还可以有第二个选择呀。嗯，我们可以在图书馆楼下，那里有一块可以遮阴的地方。哦，图书馆我觉得应该还可以吧。",
            "begin": 80,
            "end": 51640
        }
    ],
    "file_name": "2speakers_example.wav",
    "message": "",
    "uuid": "4a7a0057-d754-4121-86f7-3d801bc18c17"
}
```



