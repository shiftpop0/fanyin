# 后台 Web 实时音频翻译实施计划

> 状态：初始调研草案，已由《后台Web音频转文字实施计划-确认版》替代
>
> 编制日期：2026-08-30
>
> 用户已确认最终范围，请以项目根目录 `后台Web音频转文字实施计划-确认版.md` 为准。本文保留作为前期技术调研记录。
> 后续确认：用户已授权 8885 请求结束后清理本次上传及转换产生的临时音频；本文关于“不得删除运行期上传文件”的旧假设不再适用。

## 1. 目标

在现有 Tailect V4.1 离线 FastAPI 服务上增加一个浏览器可访问的后台 Web，支持：

1. 拖拽或选择本地音频文件上传。
2. 展示文件校验、上传、排队、识别/翻译、完成或失败状态。
3. 处理过程中持续展示新增文本，而不是等待整段音频全部完成后一次性返回。
4. 展示时间戳、说话人、原文，以及在确认需要真正翻译后展示译文。
5. 复用现有本地模型、单张 4090、单模型进程和 FIFO 并发 1 的生产约束。
6. 保持现有 `/asr*`、`/v1/audiototext`、`/translator/*` 和油猴客户端兼容，不改变既有接口合同。

## 2. 当前项目结论

### 2.1 已有能力

- 后端为 FastAPI，主入口位于 `tailect/core/api_server.py`。
- 已有文件上传转写接口：`POST /asr_raw`、`POST /asr`、`POST /v1/audiototext`。
- 已有说话人分离、ForcedAligner 时间戳、标点恢复和平台 FIFO 队列能力。
- 已有三段式流式 ASR：
  - `POST /api/stream/start`
  - `POST /api/stream/chunk`
  - `POST /api/stream/finish`
- 现有流式接口接收 `16kHz mono float32 PCM` 音频块，适合实时麦克风或客户端切块推送；默认配置 `streaming_enabled=False`。
- 当前生产结构是 `:8885 Nginx -> :6006 FastAPI`，共用同一个模型进程，首期 GPU 并发为 1。

### 2.2 缺失能力

- 仓库中没有 React、Ant Design、Vite、前端源码或前端依赖清单。
- 当前模型提供的是语音识别/转写，不存在“原文翻译到另一目标语言”的翻译引擎、目标语言参数或翻译接口。
- 现有流式 ASR 不接受完整文件后自动在服务端逐段推送结果，也没有 WebSocket/SSE 任务事件接口。
- 现有上传接口只返回最终结果，没有可供后台页面展示的上传进度、排队位置、分段进度和取消任务合同。

### 2.3 Ant Design CLI 调研结果

- 当前项目未安装 `antd`、`react`、`react-dom`，也没有构建工具。
- 本地 Ant Design CLI 可用，CLI 版本为 `6.6.2`，默认设计资料对应 Ant Design v6。
- 后台页面拟采用 CLI 已核对的组件能力：
  - `Upload.Dragger`：拖拽上传、文件列表和自定义上传流程。
  - `Progress`：上传进度和处理进度。
  - `Select`：源语言、目标语言及识别选项。
  - `Table`：持续追加显示时间戳、说话人、原文和译文。
  - `Alert`、`Result`、`Tag`、`Badge`：服务状态、错误和任务状态。
- 实施时固定 Ant Design v6 的确切版本并提交锁文件；写组件前继续使用 CLI 查询对应版本 API，写完后运行 `antd lint`。

## 3. 需要先确认的产品定义

以下问题会改变后端接口和模型方案，确认前不进入业务实现：

1. **“翻译”的含义**
   - 方案 A：只做语音转文字，识别什么语言就输出什么语言；页面命名应使用“实时转写”。
   - 方案 B：语音转文字后，再把原文翻译成指定目标语言；需要额外提供完全离线的文本翻译模型及语言范围。
2. **“实时”的含义**
   - 推荐首期：完整文件选中后上传，服务端按片段处理，通过 SSE 持续返回排队状态和新增文本。
   - 另一种含义：浏览器边读取文件边发送 PCM 块，复用现有流式 ASR；该方式更接近直播识别，但浏览器解码、断线续传和精确时间戳更复杂。
3. **访问范围与鉴权**
   - 仅内网可信用户访问，还是必须提供登录、账号与操作审计。
4. **处理后音频保留规则**
   - 长期保留、移动到项目 `./del`，或交由运维在项目外清理。根据项目规则，本次实现不会编写或执行文件删除操作。

## 4. 推荐首期方案

在没有相反确认时，推荐采用以下实现边界：

```text
浏览器（React + TypeScript + Ant Design v6）
  ├─ POST /api/web/jobs             上传文件并创建任务
  ├─ GET  /api/web/jobs/{id}        查询任务快照/断线恢复
  └─ GET  /api/web/jobs/{id}/events SSE 接收状态与增量结果
                     │
                     ▼
FastAPI Web 任务层
  ├─ 文件校验与任务状态机
  ├─ 复用现有 FIFO 并发 1
  ├─ 音频规范化与安全切片
  ├─ 调用 UnifiedService ASR
  ├─ 调用 ForcedAligner / Diarization
  └─ 若确认真实翻译，再调用独立 TranslationEngine
                     │
                     ▼
现有 Tailect_V4.1 本地模型与 GPU
```

选择 SSE 的原因：页面只需要服务器向浏览器单向推送状态和文本；SSE 原生支持事件编号、自动重连和普通 HTTP/Nginx 转发，复杂度低于 WebSocket。上传仍使用标准 multipart，浏览器可以得到真实上传进度。

现有 `/api/stream/*` 保持不变，作为以后“麦克风实时识别”或“客户端 PCM 切块”能力，不强行改造成文件任务协议。

## 5. 页面方案

首期设计为单页后台工作台，不增加无实际需求的多层导航。

### 5.1 顶部服务状态

- 显示模型别名、FastAPI 状态、GPU/CUDA、ForcedAligner、说话人分离、流式能力。
- 后端不可用或模型未就绪时禁用“开始处理”，并显示可操作的错误信息。

### 5.2 任务配置与上传

- `Upload.Dragger` 单文件上传。
- 显示允许格式、文件大小上限和文件名。
- 配置项：源语言（默认自动）、是否区分说话人、每行最大字符数。
- 只有确认“真实翻译”后才增加目标语言选择，避免当前没有翻译引擎时出现无效控件。
- 一个主要按钮“开始转写/翻译”；同一表面不放置多个主按钮。

### 5.3 实时任务状态

- 状态机：`validating -> uploading -> queued -> processing -> completed | failed`。
- 分开显示“上传进度”和“处理进度”，不以虚假百分比表示模型处理。
- 后端若只能确定阶段而不能准确计算百分比，则使用动态进度条并显示已完成片段数、音频时间位置和排队位置。
- SSE 断开时自动重连，并利用事件 ID 去重；重连失败后使用任务快照恢复。

### 5.4 结果区

- 使用表格持续追加结果行：序号、开始时间、结束时间、说话人、原文、译文（可选）。
- 新结果到达时只追加或更新对应片段，避免整表闪烁。
- 显示最终完整文本，并提供复制功能。
- 首期不默认加入 SRT/VTT/TXT 导出；如需要，在确认字段格式后作为同一任务的下载端点补充。

### 5.5 响应式与可访问性

- 桌面端优先，兼容平板宽度；小屏将结果表转换为卡片或横向滚动。
- 状态不能只靠颜色表达，必须有文字和图标。
- 键盘可操作上传区、配置项和复制按钮。
- 采用 Ant Design Token 和 4px 间距体系，不硬编码主题色或白色背景。

## 6. 后端设计

### 6.1 新增职责模块

建议新增：

- `tailect/core/web_contract.py`：任务、事件和结果行数据合同。
- `tailect/core/web_jobs.py`：内存任务注册、状态机、FIFO 调度和事件缓存。
- `tailect/core/web_router.py`：上传、任务快照、SSE 和可选下载路由。
- `tailect/core/web_processor.py`：音频切片、逐片 ASR、时间偏移合并和增量结果生成。
- `tailect/core/translation_engine.py`：仅在确认真实翻译并提供模型后新增；与 ASR 解耦。

在 `tailect/core/api_server.py` 中只注册路由和注入现有 `UnifiedService`，不继续堆积业务逻辑。

### 6.2 建议 API 合同

#### 创建任务

```http
POST /api/web/jobs
Content-Type: multipart/form-data

file=<audio>
source_language=auto
target_language=<确认真实翻译后启用>
diarize=false
max_chars=40
```

响应：

```json
{
  "job_id": "uuid",
  "status": "queued",
  "event_url": "/api/web/jobs/{job_id}/events"
}
```

#### 事件流

```http
GET /api/web/jobs/{job_id}/events
Accept: text/event-stream
```

事件类型至少包括：

- `job.snapshot`：当前完整状态，用于首次连接和恢复。
- `job.queued`：排队位置与等待状态。
- `job.started`：开始处理。
- `job.segment`：一个新增或修订后的时间戳片段。
- `job.progress`：已处理音频时长、总时长、阶段。
- `job.completed`：最终文本与统计。
- `job.failed`：稳定错误码和用户可理解的信息。

每个事件包含递增 `event_id`；服务端保存有限数量事件，浏览器用 `Last-Event-ID` 恢复。

#### 查询任务

```http
GET /api/web/jobs/{job_id}
```

返回当前任务状态和已经生成的结果行，用于刷新页面或 SSE 重连失败后的恢复。

### 6.3 实时处理策略

1. 上传完成后先做扩展名、大小、空文件和真实音频头检查。
2. 使用项目已有音频规范化能力转为 16kHz 单声道 WAV。
3. 获取音频总时长，根据现有模型安全上限按低能量点切片，片段长度不得超过当前 `pure_asr_max_chunk_seconds=300`。
4. 每个片段依次进入现有单进程 FIFO；不增加 uvicorn worker，不重复加载模型。
5. 每片识别完成后执行必要的 ForcedAligner；时间戳加上片段全局偏移，然后推送 `job.segment`。
6. `diarize=true` 时复用现有说话人分离逻辑，并保持说话人编号稳定。
7. 若确认真实翻译，对已稳定的片段执行本地文本翻译，并通过同一片段 ID 推送更新事件。
8. 全部片段完成后合并最终文本并发送 `job.completed`。

说明：逐片结果可能在相邻片段边界发生少量修订，因此前端数据合同必须允许按片段 ID 更新，而不能只允许字符串追加。

### 6.4 任务生命周期

- 首期采用单进程内存任务表，与现有单模型进程架构一致。
- 进程重启后未完成任务标记为中断，不伪装为成功。
- 任务并发严格服从现有 FIFO 并发 1 和队列上限。
- 是否支持“取消任务”需谨慎：只有模型调用可安全中断时才开放；否则只允许取消尚未开始的排队任务。
- 文件保存位置必须位于项目配置目录边界内，拒绝绝对路径和 `..`。
- 不记录 API Key、完整敏感文本或本机绝对路径到浏览器错误响应。

## 7. 前端工程方案

建议新增独立目录 `tailect/web/`：

```text
tailect/web/
  package.json
  pnpm-lock.yaml
  vite.config.ts
  tsconfig*.json
  src/
    app/
    api/
    components/
    hooks/
    pages/
    types/
    styles/
  tests/
```

技术栈：

- React + TypeScript。
- Vite 构建。
- Ant Design v6 与 `@ant-design/icons`。
- 原生 `EventSource` 处理 SSE；上传进度使用支持 `onUploadProgress` 的 HTTP 客户端或 `XMLHttpRequest` 封装。
- 状态规模较小，首期使用 React hooks 和明确的 reducer，不额外引入大型状态管理库。
- 开发环境通过 Vite proxy 转发 `/api` 和 `/health`；生产使用同源路径，避免 CORS 和密钥暴露问题。

## 8. 部署方案

推荐让 Nginx 提供前端静态文件并代理 API：

```text
浏览器 -> :8885 /
             ├─ 静态前端文件
             ├─ /api/web/* -> :6006 FastAPI
             ├─ /health    -> :6006 FastAPI
             └─ 现有平台接口保持原代理规则
```

SSE 代理必须关闭响应缓冲并延长读取超时；静态资源使用带哈希文件名和长期缓存，`index.html` 不做长期缓存。

如 8885 已被固定为只服务外部平台且不允许开放后台页面，则改用新端口，但这是部署边界变更，需要用户确认后再实施。

## 9. 安全与约束

1. 复用现有上传大小上限，并补充 MIME、扩展名、音频头和解码结果校验。
2. 生产 CORS 从 `*` 收紧为同源或明确 allowlist。
3. 复用现有 API Key/Bearer 机制；若需要多用户登录，另行设计账号、会话和审计，不用共享 API Key 假装用户系统。
4. UI 只显示稳定错误信息，详细堆栈保留在服务日志。
5. 对任务查询和 SSE 做任务 ID 校验及速率限制。
6. 不修改项目外服务或文件，不自动终止占用端口的进程。
7. 开发过程中不删除任何文件；需要废弃的文件移动到项目 `./del`。
8. 新增运行期文件保留策略必须得到用户确认；不能擅自用清理任务删除上传音频。

## 10. 实施阶段

### 阶段 A：确认范围与合同

- 确认“转写/翻译”“实时方式”“访问鉴权”“文件保留”四项决策。
- 固化 API 请求、事件、错误码和结果行 schema。
- 固化页面线框和生产访问路径。

验收：用户确认本文第 3 节，不再存在影响模型与部署路线的歧义。

### 阶段 B：后端任务与 SSE

- 实现任务合同、上传校验、内存任务表、FIFO 调度、SSE 和任务快照。
- 复用现有 ASR/ForcedAligner/Diarization，产生带全局时间戳的增量结果。
- 保持现有 API 全部兼容。

验收：假模型测试可观察完整状态机、重连、失败和增量片段；旧接口回归通过。

### 阶段 C：Ant Design 前端

- 初始化 React + TypeScript + Vite + Ant Design v6。
- 实现服务状态、上传、配置、双进度、事件流和结果表格。
- 实现断线重连、刷新恢复、重复事件去重和错误状态。
- 使用 Ant Design CLI 按锁定版本核对组件 API并执行 lint。

验收：浏览器 mock 环境下可完成成功、排队、断线、超大文件和后端失败流程。

### 阶段 D：真实翻译（条件阶段）

- 仅在确认方案 B 且离线翻译模型、语言范围、显存和性能预算明确后实施。
- 翻译引擎独立封装，不改变 ASR 结果；译文失败不能丢失原文。
- 验证与 ASR 共卡时的显存峰值和吞吐，必要时决定串行加载或独立服务。

验收：指定语言对有真实离线样本，禁止用占位文本或外网 API 冒充翻译结果。

### 阶段 E：生产构建与部署

- 构建前端静态资源，更新 Nginx 同源路由和 SSE 配置。
- 完成配置示例、启动说明和回滚说明。
- 不自动改动或重启项目外服务；生产切换前单独确认。

验收：完全断网环境下从浏览器上传到最终结果全链路通过。

## 11. 测试计划

### 11.1 后端自动测试

- 上传：正常音频、空文件、超限、伪造扩展名、损坏音频、特殊文件名。
- 状态机：合法迁移、异常迁移、排队满、排队超时、模型异常。
- SSE：事件顺序、事件 ID、心跳、断线重连、事件去重、慢客户端。
- 增量结果：片段时间偏移、边界修订、全局单调时间戳、说话人稳定编号。
- 安全：路径穿越、错误任务 ID、鉴权、速率限制、错误信息脱敏。
- 回归：`/health`、`/asr*`、`/v1/audiototext`、`/translator/*`、`/api/stream/*`。

### 11.2 前端自动测试

- 文件选择和本地校验。
- 上传进度与服务端处理状态分离。
- SSE 事件 reducer、重复事件、乱序保护和重连。
- 结果行追加/更新、错误和空状态。
- 核心页面键盘操作和可访问名称。

### 11.3 实机验收

- 单张 4090、单模型进程、FIFO 并发 1。
- 短音频、长音频、多说话人音频。
- 上传中断、SSE 中断、浏览器刷新、服务重启。
- 记录首条文本延迟、总耗时、显存峰值和队列行为。
- 完全断网运行；不得调用任何公网模型或翻译 API。

## 12. 完成标准

1. 浏览器能选择并上传合法音频，非法文件在前后端均被拒绝。
2. 页面明确显示上传、排队、处理、完成/失败，且持续收到增量文本。
3. 每条结果包含稳定 ID、文本和合法时间范围；开启说话人后编号稳定。
4. 若范围是“真实翻译”，每条结果同时显示原文与由已确认离线模型产生的译文。
5. SSE 断线可恢复，不重复或丢失已确认事件。
6. 单模型进程和现有 FIFO 约束未被破坏，旧 API 测试通过。
7. 前端依赖锁定，Ant Design lint、TypeScript、单元测试和生产构建通过。
8. Nginx/部署文档完成，完全离线实机验收通过。
9. 不删除文件，不影响项目外服务或文件。

## 13. 预计文件变更

确认计划后，预计新增或修改以下范围：

- 新增 `tailect/web/` 前端工程。
- 新增 `tailect/core/web_contract.py`。
- 新增 `tailect/core/web_jobs.py`。
- 新增 `tailect/core/web_router.py`。
- 新增 `tailect/core/web_processor.py`。
- 条件新增 `tailect/core/translation_engine.py`。
- 修改 `tailect/core/api_server.py` 注册 Web 路由。
- 修改 `tailect/core/config.py` 增加 Web 任务、SSE、保留期和访问配置。
- 新增后端与前端测试。
- 修改 `wxz/deploy/nginx_platform_8885.conf` 或新增独立 Web Nginx 配置。
- 更新项目 README 与部署/使用文档。

所有实际变更会按阶段提交前先检查 Git 状态，避开用户已有改动；需要淘汰的文件只移动到 `./del`。
