# 本地模型语音转文本 v1 接口对接评估书

版本：v0.1  
日期：2026-06-16  
评估对象：`Tailect_ASR_Win10` 本地 ASR 模型包、现有 Gradio WebUI、`语音转文本v1接口.md`  
评估结论：建议新增独立 v1 HTTP 服务适配层，不建议将现有 Gradio 私有接口直接开放给第三方。

## 1. 背景与目标

当前项目包含本地 Tailect/Qwen ASR 模型包、Gradio WebUI 启动脚本、网页音频捕获油猴脚本及接口文档。现阶段服务方需要把本地模型能力对外提供，目标接口为：

```http
POST /v1/audiototext?model=Taizhou
Content-Type: multipart/form-data
```

其中 `model` 外部公开别名暂定为：

- `Taizhou`
- `Tiantai`

返回结构需符合 `语音转文本v1接口.md`：

- 顶层字段：`code`、`language`、`data`、`file_name`、`message`、`uuid`
- `data[]` 字段：`lid`、`text`、`begin`、`end`
- `begin/end` 单位：毫秒
- `data[]` 分段策略：当前暂按“字幕行切段”，后续可根据说话人分离效果调整

## 2. 当前系统现状

### 2.1 WebUI 启动链路

当前 Windows 启动脚本为：

```text
Tailect_ASR_Win10/一键启动WebUI.bat
```

该脚本做了以下事情：

- 设置 WinPython 路径。
- 设置本地模型根目录：`models/ASR`。
- 设置 ForcedAligner 路径：`models/ForcedAligner`。
- 设置说话人分离主模型路径：`models/models/iic/speech_campplus_speaker-diarization_common`。
- 设置离线变量：`MODELSCOPE_OFFLINE=1`、`HF_HUB_OFFLINE=1`、`TRANSFORMERS_OFFLINE=1`。
- 扫描 `models/ASR` 下的模型目录，并让用户选择模型。
- 启动：

```bash
python -m tailect_asr.cli.demo \
  --asr-checkpoint "<ASR_PATH>" \
  --aligner-checkpoint "<ALIGN_PATH>" \
  --backend transformers \
  --ip 0.0.0.0 --port 7867
```

`tailect_asr.cli.demo` 只是薄封装，真实逻辑位于：

```text
Tailect_ASR_Win10/WPy64-312101/python/Lib/site-packages/qwen_asr/cli/demo.py
```

### 2.2 当前对外调用形态

现有油猴脚本调用的是 Gradio 私有协议：

1. `GET /config`
2. `POST /gradio_api/upload`
3. `POST /gradio_api/call/run`
4. `GET /gradio_api/call/run/{event_id}` 读取 SSE 完成事件

这与 v1 文档要求的单次 multipart 请求不一致：

```http
POST /v1/audiototext?model=Taizhou
```

因此现有 Gradio API 不适合作为正式对外接口直接暴露。

### 2.3 可复用的核心能力

核心 ASR 能力已经在 `qwen_asr` 包中具备：

```python
from qwen_asr import Qwen3ASRModel

model = Qwen3ASRModel.from_pretrained(...)
result = model.transcribe(
    audio="audio.wav",
    language=None,
    return_time_stamps=True,
)[0]
```

该能力可以直接用于 v1 服务内部实现，绕开 Gradio 的上传、队列、SSE 输出形态。

## 3. 实测结论

### 3.1 Tiantai 启动验证

最小加载路径验证结果：

- ASR 主模型加载成功。
- ForcedAligner 加载成功。
- CUDA 可用。
- 设备：`NVIDIA GeForce RTX 3060 Laptop GPU`。
- 加载耗时约 7.39 秒。

完整 WebUI 启动验证结果：

- `Tiantai` 可启动。
- Gradio 监听 `0.0.0.0:7867`。
- 仅出现 Gradio warning，无模型启动异常。

### 3.2 Taizhou 启动验证

最小加载路径验证结果：

- ASR 主模型加载成功。
- ForcedAligner 加载成功。
- CUDA 可用。
- 加载耗时约 5.69 秒。

完整 WebUI 启动验证结果：

- `Taizhou` 可启动。
- Gradio 监听 `0.0.0.0:7867`。
- 未复现“选择 Taizhou 模型启动失败”。

实际转写验证结果：

- 通过现有 Gradio 接口对 `prompt材料/bian.wav` 做转写。
- 识别成功。
- 语言：`Chinese`。
- 生成 6 行 SRT 字幕。

结论：当前环境下未复现 Taizhou 启动失败。该问题仍建议纳入改造范围，但应定位为“历史环境/特定启动方式/依赖环境差异/控制台日志缺失待验证”，不宜在文档中直接写成当前必现缺陷。

### 3.3 直接 Python 转写验证

直接调用 `Qwen3ASRModel.transcribe()` 结果：

- 模型：`Taizhou`
- 音频：`prompt材料/bian.wav`
- 返回语言：`Chinese`
- 返回文本正常
- 返回 33 个词/字级时间戳
- 时间戳单位为秒，例如：

```json
{
  "text": "滴",
  "start_time": 1.28,
  "end_time": 1.44
}
```

这说明 v1 的 `begin/end` 需要做如下转换：

```python
begin = int(round(start_time * 1000))
end = int(round(end_time * 1000))
```

同时，词/字级时间戳不应直接作为 v1 `data[]` 返回，应先按字幕行规则聚合。

### 3.4 说话人分离验证

直接 ModelScope pipeline 验证：

- 本地 CAM++ 主模型可加载。
- 对 `prompt材料/bian.wav` 可推理成功。
- 返回结果形如：

```python
{
    "text": [
        [0.02, 0.65, 0],
        [1.04, 2.62, 0],
        ...
    ]
}
```

WebUI `diarize=true` 验证：

- 为避免修改原始模型配置，本次将 CAM++ 主模型目录复制到 `outputs/logs/sd_model_probe_copy_20260616_150307` 作为诊断副本。
- 将 `TAILECT_SD_MODEL_PATH` 指向该副本后启动 WebUI。
- 转写成功，SRT 中出现 `[角色 0]`。

示例 SRT：

```srt
1
00:00:01,280 --> 00:00:02,240
[角色 0] 滴滴出行来电：
```

结论：当前联网环境下，说话人分离可以运行。但严格离线和服务化交付仍存在风险，见下一节。

## 4. 发现的问题与风险

### 4.1 当前离线说话人分离配置不够确定

原始 CAM++ 主配置仍包含远端模型 ID：

```json
{
  "speaker_model": "damo/speech_campplus_sv_zh-cn_16k-common",
  "change_locator": "damo/speech_campplus-transformer_scl_zh-cn_16k-common",
  "vad_model": "damo/speech_fsmn_vad_zh-cn-16k-common-pytorch"
}
```

当前 WebUI 的做法是运行时调用 `_patch_sd_configuration_for_offline()`，把这些远端 ID 改成本地绝对路径。

风险：

- 运行时需要写模型目录下的 `configuration.json`。
- 如果目标部署目录只读，修补会失败。
- 如果修补失败，代码只是打印错误并继续尝试原配置。
- 严格离线环境中，原配置的 `damo/...` 可能触发 hub/cache 解析失败。
- 当前逻辑和 WebUI 耦合，不适合对外服务复用。

建议：

- 不要依赖运行时修改原始模型配置。
- 启动前生成服务专用的本地配置副本，或在服务初始化阶段显式构造本地子模型路径。
- 启动时做 preflight 校验：主模型、speaker model、change locator、VAD model 必须全部存在。
- 失败时让服务启动失败，或返回明确健康检查错误，不要静默降级。

### 4.2 FunASR 会默认访问 PyPI 检查版本

实测说话人分离推理时出现：

```text
funasr version: 1.3.1.
Check update of funasr...
New version is available: 1.3.9.
```

源码位置：

```text
funasr/auto/auto_model.py
funasr/utils/version_checker.py
```

`AutoModel` 默认执行：

```python
check_for_update(disable=kwargs.get("disable_update", False))
```

`version_checker.py` 会请求：

```text
https://pypi.org/pypi/funasr/json
```

风险：

- 严格离线环境可能超时或异常。
- 对外服务启动/首次请求时不应发生外联。
- 现有 `MODELSCOPE_OFFLINE=1`、`HF_HUB_OFFLINE=1`、`TRANSFORMERS_OFFLINE=1` 不会自动禁止 FunASR 的 PyPI 检查。

建议：

- 在说话人分离/VAD 初始化链路中显式传 `disable_update=True`。
- 如果 ModelScope pipeline 无法透传该参数，则需要在服务入口做最小补丁或 monkey patch，禁止 `funasr.utils.version_checker.check_for_update` 外联。
- 服务启动时加外联检测日志，确保离线环境没有 `pypi.org`、`modelscope.cn`、`api.gradio.app` 请求。

### 4.3 Gradio 启动会访问外部版本接口

完整 WebUI 启动日志出现：

```text
HTTP Request: GET https://api.gradio.app/pkg-version
```

风险：

- 现有 WebUI 不适合作为严格离线服务入口。
- 即使模型本身可离线，Gradio 启动链路仍可能外联。

建议：

- v1 对外服务不依赖 Gradio。
- 若仍保留 WebUI，应单独治理 Gradio 的 analytics/version check 外联。

### 4.4 Gradio API 输出不适合作为 v1 数据源

当前 Gradio `run()` 返回：

```python
return language, text, gr.update(value=ts_payload), srt_path, srt_content
```

实测通过 Gradio API 读取时，第三个输出是 Gradio update object，而不是直接数组。

风险：

- 第三方对接如果依赖 Gradio 输出，会受到 Gradio 版本和组件协议影响。
- `data[]`、`lid`、`begin/end` 仍需二次解析 SRT，不稳定。

建议：

- v1 服务内部直接调用 `Qwen3ASRModel.transcribe()`。
- 直接拿 `r.time_stamps.items` 生成结构化字幕行。
- 说话人 ID 用 diarization 结果与字幕行时间段 overlap 匹配后填充 `data[].lid`。

### 4.5 当前说话人 ID 不是结构化字段

当前 WebUI 只在 SRT 文本中写：

```text
[角色 0] 文本
```

但 v1 要求：

```json
{
  "lid": "1",
  "text": "...",
  "begin": 80,
  "end": 51640
}
```

建议：

- v1 适配层生成字幕行时直接保留 `speaker_id`。
- `lid` 建议从 `"1"` 开始对外编号，内部 `0` 可映射为外部 `"1"`。
- 无说话人分离或匹配不到时，默认 `lid="1"` 或空字符串，需要在接口文档中约定。

### 4.6 Taizhou 启动失败当前未复现

本次验证中：

- Taizhou 最小加载成功。
- Taizhou 完整 WebUI 启动成功。
- Taizhou Gradio 转写成功。
- Taizhou 直接 Python 转写成功。
- Taizhou + diarize WebUI 路径成功。

建议仍纳入改造范围：

- 增加模型启动 preflight。
- 增加启动日志文件。
- 增加 `--model Taizhou` 非交互启动方式。
- 增加 `bf16/fp16/cpu` 诊断参数。
- 在启动失败时输出 traceback，不只停留在窗口里。

## 5. v1 服务推荐方案

### 5.1 总体架构

建议新增独立服务：

```text
客户端
  -> POST /v1/audiototext
      -> v1 API 适配层
          -> 音频保存/预处理
          -> Qwen3ASRModel.transcribe()
          -> 可选 speaker diarization
          -> 字幕行切段
          -> language/lid/begin/end 映射
      <- 标准 v1 JSON
```

不要让第三方调用 Gradio：

```text
/gradio_api/upload
/gradio_api/call/run
/gradio_api/call/run/{event_id}
```

### 5.2 推荐服务框架

运行包内已有：

- `flask=3.1.3`
- `fastapi=0.135.1`
- `uvicorn=0.42.0`
- `requests=2.32.5`
- `gradio=6.9.0`

两种可选实现：

方案 A：Flask

- 优点：简单、依赖少、易打包。
- 适合第一版同步接口。

方案 B：FastAPI + Uvicorn

- 优点：类型清晰、OpenAPI 文档方便、异步能力更好。
- 适合后续扩展队列、健康检查、模型状态接口。

第一版建议使用 Flask 或 FastAPI 均可。若目标是尽快稳定交付，Flask 更直接。

### 5.3 模型加载策略

第一版建议：一进程一模型。

示例：

```bash
一键启动API服务.bat --model Taizhou --port 8885
一键启动API服务.bat --model Tiantai --port 8886
```

请求中的 `model` 用于校验：

```http
POST /v1/audiototext?model=Taizhou
```

如果服务当前加载的是 `Taizhou`，则允许；如果请求 `Tiantai`，返回业务错误。

不建议第一版做单进程多模型热切换，原因：

- 两个 ASR 模型都是 4GB 级权重。
- ForcedAligner 也需要额外显存。
- 热切换需要释放 CUDA 显存、处理并发锁、处理请求排队。
- 故障面会明显增加。

### 5.4 请求参数

兼容文档：

```text
model: string, required
file: audio file, required
```

建议补充可选参数，但不破坏 v1：

```text
diarize: boolean, default=true 或 false，需最终确认
language: string, optional，可支持 zh/en/ja/ko/yue
max_chars: int, default=40
split_by_punctuation: boolean, default=true
```

如果必须严格遵循现有文档，则第一版只接收 `model` 和 `file`，其他配置由服务端固定。

### 5.5 返回映射

内部语言映射：

| 内部值 | v1 值 |
| --- | --- |
| `Chinese` | `zh` |
| `English` | `en` |
| `Japanese` | `ja` |
| `Korean` | `ko` |
| `Cantonese` | `yue` |

其他语言：

- 若 v1 只承诺中英日韩粤，则其他语言可返回空字符串或扩展码。
- 建议文档明确：当前 v1 对外只承诺 `zh/en/ja/ko/yue`。

时间戳映射：

```python
begin = int(round(start_time * 1000))
end = int(round(end_time * 1000))
```

说话人映射：

```python
internal speaker 0 -> lid "1"
internal speaker 1 -> lid "2"
```

无 diarization 时：

```json
"lid": "1"
```

### 5.6 字幕行切段策略

当前暂定策略：

- 使用 ASR 全文 `text`。
- 使用 ForcedAligner 词/字级时间戳。
- 按标点优先切段。
- 超过 `max_chars` 时强制切段。
- 每段 begin 取该段第一个 token 的 start_time。
- 每段 end 取该段最后一个 token 的 end_time。
- 与 diarization 时间段计算 overlap，选择重叠最大的 speaker 作为 `lid`。

这与当前 WebUI SRT 生成思路一致，但 v1 需要生成结构化结果：

```json
{
  "lid": "1",
  "text": "滴滴出行来电：",
  "begin": 1280,
  "end": 2240
}
```

## 6. 建议修改范围

### 6.1 新增 v1 API 服务入口

建议新增：

```text
Tailect_ASR_Win10/WPy64-312101/python/Lib/site-packages/tailect_asr/cli/api_v1.py
```

职责：

- 解析命令行参数：`--model`、`--host`、`--port`、`--diarize`、`--max-chars`。
- 设置离线环境。
- 启动时加载 ASR 模型和 ForcedAligner。
- 初始化说话人分离 pipeline。
- 提供 `POST /v1/audiototext`。
- 提供 `GET /health`。

### 6.2 新增 API 启动脚本

建议新增：

```text
Tailect_ASR_Win10/一键启动API服务.bat
```

职责：

- 与 WebUI bat 保持同样的离线环境变量。
- 支持选择 `Taizhou`、`Tiantai`。
- 默认监听 `0.0.0.0:8885`。
- 写启动日志到 `outputs/logs`。

### 6.3 抽出公共逻辑

建议从 `qwen_asr/cli/demo.py` 抽出或复制为服务模块：

```text
音频预处理
语言映射
字幕行切段
说话人 overlap 匹配
说话人分离本地路径解析
错误响应构造
```

建议模块名：

```text
tailect_asr/service/audio.py
tailect_asr/service/segments.py
tailect_asr/service/diarization.py
tailect_asr/service/language.py
tailect_asr/service/response.py
```

如果希望减少改动，也可以第一版全部放在 `api_v1.py` 中，但后续维护性较弱。

### 6.4 增加 preflight

建议新增：

```text
python -m tailect_asr.cli.preflight --model Taizhou
```

检查项：

- ASR 目录存在。
- `model.safetensors` 可读。
- tokenizer/config/preprocessor 文件存在。
- ForcedAligner 可加载。
- CAM++ 主模型存在。
- speaker model、change locator、VAD model 本地目录存在。
- 可选：短音频转写一次。

用于解决“Taizhou 启动失败但无日志”的定位问题。

### 6.5 增加 v1 probe

建议新增：

```text
spyware-translator/temp/tailect_v1_probe.mjs
```

或放在：

```text
Tailect_ASR_Win10/tools/tailect_v1_probe.mjs
```

职责：

- 上传 `prompt材料/bian.wav`。
- 请求 `POST /v1/audiototext?model=Taizhou`。
- 校验 HTTP 状态、`code`、`language`、`data[]`、`begin/end`。
- 输出可读摘要。

## 7. 错误码与响应建议

兼容现有 v1 文档：

成功：

```json
{
  "code": 200,
  "language": "zh",
  "data": [],
  "file_name": "audio.wav",
  "message": "",
  "uuid": "..."
}
```

业务失败：

```json
{
  "code": 500,
  "language": "",
  "data": [],
  "file_name": "audio.wav",
  "message": "Unsupported model: SenseVoiceSmall",
  "uuid": "..."
}
```

建议 HTTP 状态：

- 文档写的是“根据 response status 为 200 请求成功”，但没有明确失败时 HTTP 是否必须 200。
- 为兼容旧调用方，第一版可对业务失败仍返回 HTTP 200，body `code=500`。
- 对明显协议错误，如缺少 `file`，也可返回 HTTP 400，但需和调用方确认。

建议第一版保守处理：

- 正常识别：HTTP 200，`code=200`
- 参数/模型/识别失败：HTTP 200，`code=500`
- 服务未启动或路由不存在：HTTP 5xx/404

## 8. 并发与性能建议

第一版建议：

- 单 GPU 单模型单并发。
- 请求进入队列。
- 并发超过限制时返回排队或业务错误。
- 限制最大文件大小。
- 限制最长音频时长。
- 设置请求超时。

原因：

- ASR 主模型约 4GB 权重。
- ForcedAligner 约 1.8GB 权重。
- 说话人分离还会加载 CAM++/VAD 子模型。
- 并发推理容易导致 CUDA OOM。

建议配置：

```text
max_concurrency = 1
max_audio_seconds = 1200
max_force_align_seconds_per_chunk = 180
request_timeout_seconds = 600
```

## 9. 日志与运维建议

建议日志目录：

```text
Tailect_ASR_Win10/outputs/logs
```

日志内容：

- 服务启动参数。
- 当前加载模型别名。
- CUDA 是否可用。
- GPU 名称。
- ASR 加载耗时。
- ForcedAligner 加载耗时。
- CAM++ 加载耗时。
- 每次请求 uuid、文件名、文件大小、耗时、成功/失败。
- 失败 traceback。

建议健康检查：

```http
GET /health
```

返回：

```json
{
  "status": "ok",
  "model": "Taizhou",
  "cuda": true,
  "diarization": true
}
```

## 10. 与现有 WebUI 的关系

建议保留现有 WebUI，用于人工调试。

但对外服务应走新 v1 API：

- WebUI：人工上传、下载 SRT、调试参数。
- v1 API：第三方系统对接、稳定 JSON、固定协议。

后续可考虑让油猴脚本也从 Gradio API 切换到 v1 API，以减少系统内的接口形态数量。

## 11. 实施优先级

### P0：服务化前必须处理

- 新增 v1 API 服务。
- 明确 `Taizhou`、`Tiantai` 模型别名。
- 增加启动日志。
- 增加模型 preflight。
- 修正说话人分离离线路径策略。
- 禁用 FunASR 更新检查。
- v1 返回 `language/data[].lid/text/begin/end`。

### P1：首版联调建议处理

- 增加 `/health`。
- 增加 v1 probe。
- 增加请求队列/单并发锁。
- 增加最大文件大小和超时配置。
- 增加错误响应规范。

### P2：后续优化

- 支持多模型多进程部署。
- 支持可配置 diarize。
- 支持更完整语言码。
- 油猴脚本切换到 v1 API。
- 对字幕行切段策略做效果评估。

## 12. 验收建议

### 12.1 启动验收

- `Taizhou` API 服务可启动。
- `Tiantai` API 服务可启动。
- 启动日志中没有外联请求。
- `/health` 返回 `ok`。

### 12.2 接口验收

示例：

```bash
curl -X POST "http://127.0.0.1:8885/v1/audiototext?model=Taizhou" \
  -H "accept: application/json" \
  -F "file=@prompt材料/bian.wav;type=audio/wav"
```

验收点：

- HTTP 200。
- `code=200`。
- `uuid` 非空。
- `file_name` 等于上传文件名。
- `language` 为 `zh`。
- `data` 非空。
- 每个 `data[]` 都包含 `lid/text/begin/end`。
- `begin/end` 为整数毫秒。
- `begin <= end`。
- 字幕行文本不是逐字切分。

### 12.3 说话人分离验收

- diarize 开启时，`lid` 可稳定输出。
- 单说话人样本默认 `lid="1"`。
- 多说话人样本能输出多个 `lid`。
- 说话人分离失败时，接口要么明确失败，要么按约定降级为 `lid="1"`，不能无日志静默失败。

## 13. 当前遗留问题

1. `Taizhou` 启动失败在当前环境未复现。需要保留用户现场复现日志或通过新增 preflight 继续定位。
2. 严格离线环境尚未完整模拟。当前验证环境有网络，因此 FunASR 和 Gradio 的外联风险需要在无网络环境下再验一次。
3. “按字幕行切段”是当前暂定策略。后续需要用真实多说话人长音频验证切段效果。
4. v1 文档没有定义可选参数、失败 HTTP 状态、文件大小限制、并发策略，需要在对接前补充服务方约定。
5. 现有 WebUI 临时文件清理使用 `os.remove()`，如果后续把同逻辑迁入服务，需要按当前项目约束改为保留或移动到 `del/`。

## 14. 总结

当前模型核心能力已经具备：`Taizhou`、`Tiantai` 均可加载，`Taizhou` 可完成实际转写，ForcedAligner 可返回时间戳，说话人分离在当前环境可运行。

真正需要改造的是服务化边界：

- 不要直接开放 Gradio 私有接口。
- 新增稳定的 `/v1/audiototext` HTTP 服务。
- 把时间戳和说话人信息结构化。
- 把离线依赖和外联检查治理干净。
- 给 Taizhou/Tiantai 启动失败留出 preflight 和日志诊断能力。

按上述范围实施后，项目可以较稳妥地作为服务提供方对外提供本地 ASR 能力。
